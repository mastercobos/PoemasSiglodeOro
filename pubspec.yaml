name: poemas_siglo_de_oro
description: Antología poética con índice por autor.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  shared_preferences: ^2.3.0
  provider: ^6.1.2

flutter:
  uses-material-design: true
  assets:
    - assets/poemas.json
dev_dependencies:
  flutter_lints: ^4.0.0
